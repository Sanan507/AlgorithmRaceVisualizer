import requests

GITHUB_TOKEN = "" # Assuming the environment doesn't provide a GH token directly, I'll need to figure out how to authenticate or post as Jules.
# But wait, my memory says "When acting as the daily review task: List open PRs starting with 'jules/', pull the branch, run tests/build/lint, and leave one summary review comment."
# The `gh` CLI tool is not available in the environment; use `curl` to interact with the GitHub API.
